<div class="row padding">
    <a href="{{ path('admin.formation.create') }}" class="btn btn-md bg-success px-5 text-white"
       title="Ajouter une nouvelle formation"><i class="fa fa-plus pr-3" aria-hidden="true"></i>Nouvelle formation</a>
</div>

{% for formation in formations %}
    <div class="card my-5">
        <div class="row no-gutters shadow">
            <div class="col-md-4">
                {% if formation.filename %}
                    <img class="img-fluid"
                         src="{{ vich_uploader_asset(formation, 'imageFile') | imagine_filter('paysage') }}"
                         alt="card-img-top">
                {% else %}
                    <img class="img-fluid"
                         src="{{ 'images/no-image.png' | imagine_filter('paysage') }}"
                         alt="card-img-top">
                {% endif %}
            </div>
            <div class="col-md-8">
                <div class="card-body">
                    <div class="text-right pb-2">
                        <a href="{{ path('admin.formation.edit', {id: formation.id }) }}"
                           class="btn btn-sm btn-outline-primary"><i
                                    class="fa fa-pencil" aria-hidden="true"></i></a>
                        <form method="post" action="{{ path('admin.formation.delete', {id: formation.id}) }}"
                              style="display: inline-block"
                              onsubmit="return confirm('Etes-vous sur de vouloir supprimer ces informations?')">
                            <input type="hidden" name="_method" value="DELETE">
                            <input type="hidden" name="_token"
                                   value="{{ csrf_token('delete' ~ formation.id) }}">
                            <button class="btn btn-sm btn-outline-danger"><i class="fa fa-trash-o"
                                                                             aria-hidden="true"></i>
                            </button>
                        </form>
                    </div>
                    <h5 class="card-title">{{ formation.content }}</h5>
                    <p class="card-title">{{ formation.schoolname }}<small class="text-muted float-right">{{ formation.schooldate }}</small></p>
                    <p class="card-text"><small class="text-muted">{{ formation.path }}</small></p>
                </div>
            </div>
        </div>
    </div>
{% endfor %}
