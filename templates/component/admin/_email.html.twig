<div class="padding">
    <div class="row">
        <div class="col-12">
            <div class="card shadow">
                <div class="card-header font-weight-bold bg-white text-blue border-bottom-blue text-center px-3 py-2">
                    Contact
                </div>
                <table class="dataTable table table-striped table-borderless text-center bg-white m-0">
                    <thead>
                    <tr>
                        <th class="td-25">Nom</th>
                        <th class="td-25">Prénom</th>
                        <th class="td-30">Email</th>
                        <th class="td-10"></th>
                    </tr>
                    </thead>
                    <tbody class="scrollbar scrollbar-black bordered-black square thin table-height-formation">
                    {% for email in emails %}
                        <tr>
                            <td class="td-25">{{ email.lastname }}</td>
                            <td class="td-25">{{ email.firstname }}</td>
                            <td class="td-30">{{ email.email }}</td>
                            <td class="td-10">
                                <form method="post" action="{{ path('admin.email.delete', {id: email.id}) }}"
                                      style="display: inline-block"
                                      onsubmit="return confirm('Etes-vous sur de vouloir supprimer ces informations?')">
                                    <input type="hidden" name="_method" value="DELETE">
                                    <input type="hidden" name="_token"
                                           value="{{ csrf_token('delete' ~ email.id) }}">
                                    <button class="btn btn-sm btn-outline-danger"><i class="fa fa-trash-o"
                                                                                     aria-hidden="true"></i>
                                    </button>
                                </form>
                            </td>
                        </tr>
                        <tr>
                            <td colspan="4" class="text-left"><strong>Message : </strong> {{ contact.content }}</td>
                        </tr>
                    {% endfor %}
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>
