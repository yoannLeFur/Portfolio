{% extends 'base.html.twig' %}
{% block title 'Portfolio > Admin' %}

{% block body %}
    <div class="row m-0 padding-admin">
        <div class="col-4"></div>
        <div class="col-4">
            <h1>Administration</h1>
        </div>
        <div class="col-4">
            <div class="offset-0 col-12 offset-lg-2 col-lg-8 text-right">
                <a href="{{ path('admin.contact') }}" class="btn btn-md bg-success px-5 text-white">Email<i class="fa fa-chevron-right pl-3" aria-hidden="true"></i></a>
            </div>
        </div>
    </div>

    <div class="row">
        <div class="offset-2 col-8">
            {% for message in app.flashes('success') %}
                <div class="alert alert-success">
                    {{ message }}
                </div>
            {% endfor %}

            {% for message in app.flashes('primary') %}
                <div class="alert alert-primary">
                    {{ message }}
                </div>
            {% endfor %}

            {% for message in app.flashes('danger') %}
                <div class="alert alert-danger">
                    {{ message }}
                </div>
            {% endfor %}
        </div>
    </div>

    <div class="padding">
        <div class="row">
            <div class="col-6 p-0">
                <div class="card shadow ">
                    <div class="card-header font-weight-bold bg-white text-blue border-bottom-blue text-center px-3 py-2">
                        Home
                    </div>
                    <table class="table table-fixed table-borderless text-center bg-white m-0">
                        <thead>
                        <tr>
                            <th class="col-2">Image</th>
                            <th class="col-2">Titre</th>
                            <th class="col-6">contenu</th>
                            <th class="col-2">
                                <a href="{{ path('admin.home.create') }}" class="btn btn-sm btn-outline-success"
                                   title="Ajouter"><i class="fa fa-plus" aria-hidden="true"></i></a>
                            </th>
                        </tr>
                        </thead>
                        <tbody>
                        {% for home in homes %}
                            <tr style="width: 100%">
                                <td class="col-2">
                                    {% if home.filename %}
                                        <img class="img-fluid rounded pb-2"
                                             src="{{ vich_uploader_asset(home, 'imageFile') | imagine_filter('small') }}"
                                             alt="card-img-top">
                                    {% else %}
                                        <img class="img-fluid rounded pb-2"
                                             src="{{ 'images/no-image.png' | imagine_filter('small') }}"
                                             alt="card-img-top">
                                    {% endif %}
                                </td>
                                <td class="col-2">{{ home.title }}</td>
                                <td class="col-6">{{ home.content }}</td>
                                <td class="col-2">
                                    <a href="{{ path('admin.home.edit', {id: home.id }) }}"
                                       class="btn btn-sm btn-outline-primary"><i
                                                class="fa fa-pencil" aria-hidden="true"></i></a>
                                    <form method="post" action="{{ path('admin.home.delete', {id: home.id}) }}"
                                          style="display: inline-block"
                                          onsubmit="return confirm('Etes-vous sur de vouloir supprimer ces informations?')">
                                        <input type="hidden" name="_method" value="DELETE">
                                        <input type="hidden" name="_token"
                                               value="{{ csrf_token('delete' ~ home.id) }}">
                                        <button class="btn btn-sm btn-outline-danger"><i class="fa fa-trash-o"
                                                                                         aria-hidden="true"></i>
                                        </button>
                                    </form>
                                </td>

                            </tr>
                        {% endfor %}
                        </tbody>
                    </table>
                </div>
            </div>
            <div class="col-6 p-0">
                <div class="card shadow">
                    <div class="card-header font-weight-bold bg-white text-blue border-bottom-blue text-center px-3 py-2">
                        Compétences
                    </div>
                    <table class="dataTable table table-striped table-borderless text-center bg-white m-0">
                        <thead>
                        <tr>
                            <th>Image</th>
                            <th>Titre</th>
                            <th>
                                <a href="{{ path('admin.skill.create') }}" class="btn btn-sm btn-outline-success"
                                   title="Ajouter"><i class="fa fa-plus" aria-hidden="true"></i></a>
                            </th>
                        </tr>
                        </thead>
                        <tbody class="scrollbar scrollbar-black bordered-black square thin table-height-skill">
                        {% for skill in skills %}
                            <tr>
                                <td style="width:20%">
                                    {% if skill.filename %}
                                        <img class="img-fluid rounded pb-2"
                                             src="{{ vich_uploader_asset(skill, 'imageFile') | imagine_filter('smallPaysage') }}"
                                             alt="card-img-top">
                                    {% else %}
                                        <img class="img-fluid rounded pb-2"
                                             src="{{ 'images/no-image.png' | imagine_filter('smallPaysage') }}"
                                             alt="card-img-top">
                                    {% endif %}
                                </td>
                                <td style="width:60%">{{ skill.title }}</td>
                                <td style="width:20%">
                                    <a href="{{ path('admin.skill.edit', {id: skill.id }) }}"
                                       class="btn btn-sm btn-outline-primary"><i
                                                class="fa fa-pencil" aria-hidden="true"></i></a>
                                    <form method="post" action="{{ path('admin.skill.delete', {id: skill.id}) }}"
                                          style="display: inline-block"
                                          onsubmit="return confirm('Etes-vous sur de vouloir supprimer ces informations?')">
                                        <input type="hidden" name="_method" value="DELETE">
                                        <input type="hidden" name="_token"
                                               value="{{ csrf_token('delete' ~ skill.id) }}">
                                        <button class="btn btn-sm btn-outline-danger"><i class="fa fa-trash-o"
                                                                                         aria-hidden="true"></i>
                                        </button>
                                    </form>
                                </td>
                            </tr>
                        {% endfor %}
                        </tbody>
                    </table>
                </div>
            </div>
            <div class="col-12 p-0">
                <div class="card shadow">
                    <div class="card-header font-weight-bold bg-white text-blue border-bottom-blue text-center px-3 py-2">
                        Présentation
                    </div>
                    <table class="table table-striped table-borderless text-center bg-white m-0">
                        <thead>
                        <tr>
                            <th>Image</th>
                            <th>Titre</th>
                            <th>Contenu</th>
                            <th>Contenu 1</th>
                            <th>Contenu 2</th>
                            <th>
                                <a href="{{ path('admin.about.create') }}" class="btn btn-sm btn-outline-success"
                                   title="Ajouter"><i class="fa fa-plus" aria-hidden="true"></i></a>
                            </th>
                        </tr>
                        </thead>
                        <tbody>
                        {% for about in abouts %}
                            <tr>
                                <td style="width:10%">
                                    {% if about.filename %}
                                        <img class="img-fluid rounded pb-2"
                                             src="{{ vich_uploader_asset(about, 'imageFile') | imagine_filter('small') }}"
                                             alt="card-img-top">
                                    {% else %}
                                        <img class="img-fluid rounded pb-2"
                                             src="{{ 'images/no-image.png' | imagine_filter('small') }}"
                                             alt="card-img-top">
                                    {% endif %}
                                </td>
                                <td style="width:20%">{{ about.title }}</td>
                                <td class="text-left" style="width:25%">{{ about.content }}</td>
                                <td class="text-left" style="width:25%">{{ about.content1 }}</td>
                                <td class="text-left" style="width:10%">{{ about.content2 }}</td>
                                <td style="width:10%">
                                    <a href="{{ path('admin.about.edit', {id: about.id }) }}"
                                       class="btn btn-sm btn-outline-primary"><i
                                                class="fa fa-pencil" aria-hidden="true"></i></a>
                                    <form method="post" action="{{ path('admin.about.delete', {id: about.id}) }}"
                                          style="display: inline-block"
                                          onsubmit="return confirm('Etes-vous sur de vouloir supprimer ces informations?')">
                                        <input type="hidden" name="_method" value="DELETE">
                                        <input type="hidden" name="_token"
                                               value="{{ csrf_token('delete' ~ about.id) }}">
                                        <button class="btn btn-sm btn-outline-danger"><i class="fa fa-trash-o"
                                                                                         aria-hidden="true"></i>
                                        </button>
                                    </form>
                                </td>
                            </tr>
                        {% endfor %}
                        </tbody>
                    </table>
                </div>
            </div>
            <div class="col-12 p-0">
                <div class="card shadow ">
                    <div class="card-header font-weight-bold bg-white text-blue border-bottom-blue text-center px-3 py-2">
                        Formation
                    </div>
                    <table class="dataTable table table-striped table-borderless text-center bg-white m-0">
                        <thead>
                        <tr>
                            <th>Image</th>
                            <th>contenu</th>
                            <th>Ecole</th>
                            <th>Date</th>
                            <th>Url</th>
                            <th>
                                <a href="{{ path('admin.formation.create') }}" class="btn btn-sm btn-outline-success"
                                   title="Ajouter"><i class="fa fa-plus" aria-hidden="true"></i></a>
                            </th>
                        </tr>
                        </thead>
                        <tbody class="scrollbar scrollbar-black bordered-black square thin table-height-formation">
                        {% for formation in formations %}
                            <tr>
                                <td style="width:10%">
                                    {% if formation.filename %}
                                        <img class="img-fluid rounded pb-2"
                                             src="{{ vich_uploader_asset(formation, 'imageFile') | imagine_filter('small') }}"
                                             alt="card-img-top">
                                    {% else %}
                                        <img class="img-fluid rounded pb-2"
                                             src="{{ 'images/no-image.png' | imagine_filter('small') }}"
                                             alt="card-img-top">
                                    {% endif %}
                                </td>
                                <td style="width:20%">{{ formation.content }}</td>
                                <td style="width:20%">{{ formation.schoolname }}</td>
                                <td style="width:20%">{{ formation.schooldate }}</td>
                                <td style="width:20%">{{ formation.path }}</td>
                                <td style="width:10%">
                                    <a href="{{ path('admin.formation.edit', {id: formation.id }) }}"
                                       class="btn btn-sm btn-outline-primary"><i
                                                class="fa fa-pencil" aria-hidden="true"></i></a>
                                    <form method="post" action="{{ path('admin.formation.delete', {id: formation.id}) }}"
                                          style="display: inline-block"
                                          onsubmit="return confirm('Etes-vous sur de vouloir supprimer cette formation?')">
                                        <input type="hidden" name="_method" value="DELETE">
                                        <input type="hidden" name="_token"
                                               value="{{ csrf_token('delete' ~ formation.id) }}">
                                        <button class="btn btn-sm btn-outline-danger"><i class="fa fa-trash-o"
                                                                                         aria-hidden="true"></i>
                                        </button>
                                    </form>
                                </td>
                            </tr>
                        {% endfor %}
                        </tbody>
                    </table>
                </div>
            </div>
            <div class="col-12 p-0">
                <div class="card shadow ">
                    <div class="card-header font-weight-bold bg-white text-blue border-bottom-blue text-center px-3 py-2">
                        Projets
                    </div>
                    <table class="dataTable table table-striped table-borderless text-center bg-white m-0" style="width:100%">
                        <thead>
                        <tr>
                            <th>Image</th>
                            <th>Titre</th>
                            <th>contenu</th>
                            <th>Url</th>
                            <th>
                                <a href="{{ path('admin.projet.create') }}" class="btn btn-sm btn-outline-success"
                                   title="Ajouter"><i class="fa fa-plus" aria-hidden="true"></i></a>
                            </th>
                        </tr>
                        </thead>
                        <tbody class="scrollbar scrollbar-black bordered-black square thin table-height-projet">
                        {% for projet in projets %}
                            <tr>
                                <td style="width:10%">
                                    {% if projet.filename %}
                                        <img class="img-fluid rounded pb-2"
                                             src="{{ vich_uploader_asset(projet, 'imageFile') | imagine_filter('smallPaysage') }}"
                                             alt="card-img-top">
                                    {% else %}
                                        <img class="img-fluid rounded pb-2"
                                             src="{{ 'images/no-image.png' | imagine_filter('smallPaysage') }}"
                                             alt="card-img-top">
                                    {% endif %}
                                </td>
                                <td style="width:10%">{{ projet.title }}</td>
                                <td style="width:60%">{{ projet.content }}</td>
                                <td style="width:10%">{{ projet.path }}</td>
                                <td style="width:10%">
                                    <a href="{{ path('admin.projet.edit', {id: projet.id }) }}"
                                       class="btn btn-sm btn-outline-primary"><i
                                                class="fa fa-pencil" aria-hidden="true"></i></a>
                                    <form method="post" action="{{ path('admin.projet.delete', {id: projet.id}) }}"
                                          style="display: inline-block"
                                          onsubmit="return confirm('Etes-vous sur de vouloir supprimer ces informations?')">
                                        <input type="hidden" name="_method" value="DELETE">
                                        <input type="hidden" name="_token"
                                               value="{{ csrf_token('delete' ~ projet.id) }}">
                                        <button class="btn btn-sm btn-outline-danger"><i class="fa fa-trash-o"
                                                                                         aria-hidden="true"></i>
                                        </button>
                                    </form>
                                </td>
                            </tr>
                        {% endfor %}
                        </tbody>
                    </table>
                </div>
            </div>
            <div class="col-12 p-0">
                <div class="card shadow">
                    <div class="card-header font-weight-bold bg-white text-blue border-bottom-blue text-center px-3 py-2">
                        Expériences
                    </div>
                    <table class="dataTable table table-striped table-borderless text-center bg-white m-0">
                        <thead>
                        <tr>
                            <th>Logo</th>
                            <th>Titre</th>
                            <th>contenu</th>
                            <th>date</th>
                            <th>Url</th>
                            <th>
                                <a href="{{ path('admin.experience.create') }}" class="btn btn-sm btn-outline-success"
                                   title="Ajouter"><i class="fa fa-plus" aria-hidden="true"></i></a>
                            </th>
                        </tr>
                        </thead>
                        <tbody class="scrollbar scrollbar-black bordered-black square thin table-height-experience">
                        {% for experience in experiences %}
                            <tr>
                                <td style="width:10%">
                                    {% if experience.filename %}
                                        <img class="img-fluid rounded pb-2"
                                             src="{{ vich_uploader_asset(experience, 'imageFile') | imagine_filter('small') }}"
                                             alt="card-img-top">
                                    {% else %}
                                        <img class="img-fluid rounded pb-2"
                                             src="{{ 'images/no-image.png' | imagine_filter('small') }}"
                                             alt="card-img-top">
                                    {% endif %}
                                </td>
                                <td style="width:10%">{{ experience.title }}</td>
                                <td style="width:40%">{{ experience.content }}</td>
                                <td style="width:10%">{{ experience.experiencedate }}</td>
                                <td style="width:20%">{{ experience.path }}</td>
                                <td style="width:10%">
                                    <a href="{{ path('admin.experience.edit', {id: experience.id }) }}"
                                       class="btn btn-sm btn-outline-primary"><i
                                                class="fa fa-pencil" aria-hidden="true"></i></a>
                                    <form method="post" action="{{ path('admin.experience.delete', {id: experience.id}) }}"
                                          style="display: inline-block"
                                          onsubmit="return confirm('Etes-vous sur de vouloir supprimer ces informations?')">
                                        <input type="hidden" name="_method" value="DELETE">
                                        <input type="hidden" name="_token"
                                               value="{{ csrf_token('delete' ~ experience.id) }}">
                                        <button class="btn btn-sm btn-outline-danger"><i class="fa fa-trash-o"
                                                                                         aria-hidden="true"></i>
                                        </button>
                                    </form>
                                </td>
                            </tr>
                        {% endfor %}
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
